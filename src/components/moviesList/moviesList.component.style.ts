import { createUseStyles } from 'react-jss';

const style = createUseStyles({
  moviesAmount: {
    fontSize: '1.25rem',
  },
  moviesAmountText: {
    marginLeft: '0.5rem',
    fontWeight: 100,
    fontSize: '1.2rem',
  },
});

export default style;
